# DEP-PM Platform — Backend dependencies (Sprint 4)
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.1
anthropic==0.42.0
python-dotenv==1.0.1
httpx==0.28.1              # deploy dispatcher (GitHub API) + test client

# --- Team Mode providers (Sprint 4) — lazy-imported, ระบบรันได้แม้ไม่ตั้ง key ---
openai==1.59.7
google-genai==1.2.0

# --- PostgreSQL (staging/prod — ADR-01) ---
psycopg[binary]==3.2.4

# --- dev / test ---
pytest==8.3.4
pytest-asyncio==0.25.0
